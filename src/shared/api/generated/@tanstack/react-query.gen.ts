// This file is auto-generated by @hey-api/openapi-ts

import {
  type InfiniteData,
  infiniteQueryOptions,
  queryOptions,
  type UseMutationOptions,
} from '@tanstack/react-query';

import { client } from '../client.gen';
import {
  createSample,
  getAllSamples,
  getChannel,
  getChannels,
  getSampleById,
  googleAuth,
  linkGoogle,
  login,
  loginMethods,
  logout,
  type Options,
  presignOnboardingPerformanceFiles,
  refresh,
  sendSignupCode,
  signup,
  signupGoogle,
  submitOnboarding,
  verifySignupCode,
} from '../sdk.gen';
import type {
  CreateSampleData,
  CreateSampleError,
  CreateSampleResponse,
  GetAllSamplesData,
  GetAllSamplesError,
  GetAllSamplesResponse,
  GetChannelData,
  GetChannelError,
  GetChannelResponse,
  GetChannelsData,
  GetChannelsError,
  GetChannelsResponse,
  GetSampleByIdData,
  GetSampleByIdError,
  GetSampleByIdResponse,
  GoogleAuthData,
  GoogleAuthError,
  GoogleAuthResponse,
  LinkGoogleData,
  LinkGoogleError,
  LinkGoogleResponse,
  LoginData,
  LoginError,
  LoginMethodsData,
  LoginMethodsError,
  LoginMethodsResponse,
  LoginResponse,
  LogoutData,
  LogoutError,
  LogoutResponse,
  PresignOnboardingPerformanceFilesData,
  PresignOnboardingPerformanceFilesError,
  PresignOnboardingPerformanceFilesResponse,
  RefreshData,
  RefreshError,
  RefreshResponse,
  SendSignupCodeData,
  SendSignupCodeError,
  SendSignupCodeResponse,
  SignupData,
  SignupError,
  SignupGoogleData,
  SignupGoogleError,
  SignupGoogleResponse,
  SignupResponse,
  SubmitOnboardingData,
  SubmitOnboardingError,
  SubmitOnboardingResponse,
  VerifySignupCodeData,
  VerifySignupCodeError,
  VerifySignupCodeResponse,
} from '../types.gen';

export type QueryKey<TOptions extends Options> = [
  Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
    _id: string;
    _infinite?: boolean;
    tags?: ReadonlyArray<string>;
  },
];

const createQueryKey = <TOptions extends Options>(
  id: string,
  options?: TOptions,
  infinite?: boolean,
  tags?: ReadonlyArray<string>,
): [QueryKey<TOptions>[0]] => {
  const params: QueryKey<TOptions>[0] = {
    _id: id,
    baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl,
  } as QueryKey<TOptions>[0];
  if (infinite) {
    params._infinite = infinite;
  }
  if (tags) {
    params.tags = tags;
  }
  if (options?.body) {
    params.body = options.body;
  }
  if (options?.headers) {
    params.headers = options.headers;
  }
  if (options?.path) {
    params.path = options.path;
  }
  if (options?.query) {
    params.query = options.query;
  }
  return [params];
};

export const getAllSamplesQueryKey = (options?: Options<GetAllSamplesData>) =>
  createQueryKey('getAllSamples', options);

/**
 * 샘플 목록 조회
 *
 * 전체 샘플 목록을 조회한다.
 */
export const getAllSamplesOptions = (options?: Options<GetAllSamplesData>) =>
  queryOptions<
    GetAllSamplesResponse,
    GetAllSamplesError,
    GetAllSamplesResponse,
    ReturnType<typeof getAllSamplesQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getAllSamples({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getAllSamplesQueryKey(options),
  });

/**
 * 샘플 생성
 *
 * 이름을 받아 새로운 샘플을 생성한다.
 */
export const createSampleMutation = (
  options?: Partial<Options<CreateSampleData>>,
): UseMutationOptions<CreateSampleResponse, CreateSampleError, Options<CreateSampleData>> => {
  const mutationOptions: UseMutationOptions<
    CreateSampleResponse,
    CreateSampleError,
    Options<CreateSampleData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await createSample({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 온보딩 제출
 *
 * 로그인 여부와 관계없이 제출할 수 있다. 로그인 상태에서 다시 제출하면 이전 제출을 대체하고, 비로그인은 대체 없이 각각 저장된다.
 */
export const submitOnboardingMutation = (
  options?: Partial<Options<SubmitOnboardingData>>,
): UseMutationOptions<
  SubmitOnboardingResponse,
  SubmitOnboardingError,
  Options<SubmitOnboardingData>
> => {
  const mutationOptions: UseMutationOptions<
    SubmitOnboardingResponse,
    SubmitOnboardingError,
    Options<SubmitOnboardingData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await submitOnboarding({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 성과파일 presigned URL 발급
 *
 * 성과파일(xlsx/csv) 업로드용 presigned PUT URL을 로그인 여부와 관계없이 발급한다. PUT 요청 시 응답의 contentType 값과 x-amz-tagging: retain=pending 헤더를 그대로 보내야 한다.
 */
export const presignOnboardingPerformanceFilesMutation = (
  options?: Partial<Options<PresignOnboardingPerformanceFilesData>>,
): UseMutationOptions<
  PresignOnboardingPerformanceFilesResponse,
  PresignOnboardingPerformanceFilesError,
  Options<PresignOnboardingPerformanceFilesData>
> => {
  const mutationOptions: UseMutationOptions<
    PresignOnboardingPerformanceFilesResponse,
    PresignOnboardingPerformanceFilesError,
    Options<PresignOnboardingPerformanceFilesData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await presignOnboardingPerformanceFiles({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 회원가입 최종 제출
 *
 * 이메일 인증 완료 후 로컬 계정을 생성한다. 인증 미완료 시 400, 이메일 중복 시 409.
 */
export const signupMutation = (
  options?: Partial<Options<SignupData>>,
): UseMutationOptions<SignupResponse, SignupError, Options<SignupData>> => {
  const mutationOptions: UseMutationOptions<SignupResponse, SignupError, Options<SignupData>> = {
    mutationFn: async (fnOptions) => {
      const { data } = await signup({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 구글 최종 회원가입
 *
 * POST /auth/google 이 내려준 signupToken 과 추가 프로필(닉네임/회사명/직무/약관 동의)로 신규 회원가입을 완료하고 토큰을 발급한다. 가입에 성공하면 signupToken 은 즉시 폐기되어 재사용 불가능.
 */
export const signupGoogleMutation = (
  options?: Partial<Options<SignupGoogleData>>,
): UseMutationOptions<SignupGoogleResponse, SignupGoogleError, Options<SignupGoogleData>> => {
  const mutationOptions: UseMutationOptions<
    SignupGoogleResponse,
    SignupGoogleError,
    Options<SignupGoogleData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await signupGoogle({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 회원가입 이메일 인증코드 발송
 *
 * 가입할 이메일로 6자리 인증코드를 발송한다. 로컬로 이미 가입된 이메일: 409. 구글로만 가입된 이메일: 발송하지 않고 200 과 code: EMAIL_ALREADY_USED_WITH_GOOGLE. 그 외: 발송 후 200.
 */
export const sendSignupCodeMutation = (
  options?: Partial<Options<SendSignupCodeData>>,
): UseMutationOptions<SendSignupCodeResponse, SendSignupCodeError, Options<SendSignupCodeData>> => {
  const mutationOptions: UseMutationOptions<
    SendSignupCodeResponse,
    SendSignupCodeError,
    Options<SendSignupCodeData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await sendSignupCode({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 회원가입 이메일 인증코드 확인
 *
 * 발송된 코드를 검증하고 인증완료 상태로 전환한다.
 */
export const verifySignupCodeMutation = (
  options?: Partial<Options<VerifySignupCodeData>>,
): UseMutationOptions<
  VerifySignupCodeResponse,
  VerifySignupCodeError,
  Options<VerifySignupCodeData>
> => {
  const mutationOptions: UseMutationOptions<
    VerifySignupCodeResponse,
    VerifySignupCodeError,
    Options<VerifySignupCodeData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await verifySignupCode({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 토큰 재발급
 *
 * Refresh Token 을 회전시켜 새 access/refresh 토큰 쌍을 발급한다. 기존 Refresh Token 은 즉시 폐기된다. 이미 회전된 토큰을 다시 제출하면 재사용으로 보고 해당 세션(family) 전체를 폐기하므로, 동시에 여러 번 호출하지 말고 한 번만 보낸다. Access Token 이 만료된 상태에서 호출하므로 인증이 필요 없다. refreshTokenExpiresIn 은 로그인 후 90일에 가까워질수록 짧아진다.
 */
export const refreshMutation = (
  options?: Partial<Options<RefreshData>>,
): UseMutationOptions<RefreshResponse, RefreshError, Options<RefreshData>> => {
  const mutationOptions: UseMutationOptions<RefreshResponse, RefreshError, Options<RefreshData>> = {
    mutationFn: async (fnOptions) => {
      const { data } = await refresh({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 로그아웃
 *
 * 제출한 Refresh Token 의 세션(family)을 폐기한다. 인증된 사용자만 호출할 수 있고, 토큰 소유자가 인증 사용자와 다르면 401. 이미 폐기된 세션이어도 200 을 반환한다(멱등).
 */
export const logoutMutation = (
  options?: Partial<Options<LogoutData>>,
): UseMutationOptions<LogoutResponse, LogoutError, Options<LogoutData>> => {
  const mutationOptions: UseMutationOptions<LogoutResponse, LogoutError, Options<LogoutData>> = {
    mutationFn: async (fnOptions) => {
      const { data } = await logout({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 로컬 로그인
 *
 * 이메일/비밀번호로 로그인하고 access/refresh 토큰을 발급한다. 자격증명이 올바르지 않으면 401. 구글로만 가입되어 로컬 인증정보가 없는 이메일이면 AUTH-010 으로 별도 응답한다.
 */
export const loginMutation = (
  options?: Partial<Options<LoginData>>,
): UseMutationOptions<LoginResponse, LoginError, Options<LoginData>> => {
  const mutationOptions: UseMutationOptions<LoginResponse, LoginError, Options<LoginData>> = {
    mutationFn: async (fnOptions) => {
      const { data } = await login({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 로그인 수단 조회
 *
 * 이메일로 사용 가능한 로그인 수단을 조회한다. 비밀번호 입력 전 화면 분기용. methods [LOCAL]: 비밀번호 입력창. [LOCAL, GOOGLE]: 비밀번호 입력창과 구글 버튼. [GOOGLE]: 구글로 가입된 계정 안내. []: 미가입 -> 회원가입 화면.
 */
export const loginMethodsMutation = (
  options?: Partial<Options<LoginMethodsData>>,
): UseMutationOptions<LoginMethodsResponse, LoginMethodsError, Options<LoginMethodsData>> => {
  const mutationOptions: UseMutationOptions<
    LoginMethodsResponse,
    LoginMethodsError,
    Options<LoginMethodsData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await loginMethods({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 구글 인증 진입
 *
 * 구글 idToken 을 검증하고 계정 상태에 따라 status 로 분기한다. LOGIN: 토큰 발급. LINK_REQUIRED: linkRequired 와 email 을 내려주며, 사용자 확인 후 POST /auth/google/link 호출. SIGNUP_REQUIRED: signupRequired 와 일회성 signupToken, 프리필 값을 내려준다.
 */
export const googleAuthMutation = (
  options?: Partial<Options<GoogleAuthData>>,
): UseMutationOptions<GoogleAuthResponse, GoogleAuthError, Options<GoogleAuthData>> => {
  const mutationOptions: UseMutationOptions<
    GoogleAuthResponse,
    GoogleAuthError,
    Options<GoogleAuthData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await googleAuth({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * 구글 계정 연결 확인
 *
 * linkRequired:true 응답을 받고 사용자가 '예'를 선택했을 때만 호출한다. 진입 때 보낸 idToken 을 재전송하면 재검증 후 같은 이메일의 로컬 계정에 구글 로그인을 연결하고 토큰을 발급한다.
 */
export const linkGoogleMutation = (
  options?: Partial<Options<LinkGoogleData>>,
): UseMutationOptions<LinkGoogleResponse, LinkGoogleError, Options<LinkGoogleData>> => {
  const mutationOptions: UseMutationOptions<
    LinkGoogleResponse,
    LinkGoogleError,
    Options<LinkGoogleData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await linkGoogle({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const getSampleByIdQueryKey = (options: Options<GetSampleByIdData>) =>
  createQueryKey('getSampleById', options);

/**
 * 샘플 단건 조회
 *
 * 식별자로 샘플을 조회한다.
 */
export const getSampleByIdOptions = (options: Options<GetSampleByIdData>) =>
  queryOptions<
    GetSampleByIdResponse,
    GetSampleByIdError,
    GetSampleByIdResponse,
    ReturnType<typeof getSampleByIdQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getSampleById({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getSampleByIdQueryKey(options),
  });

export const getChannelsQueryKey = (options?: Options<GetChannelsData>) =>
  createQueryKey('getChannels', options);

/**
 * 채널 목록 조회
 *
 * 채널을 페이지 단위로 조회한다. 미지정 시 이름순 12개. name 지정 시 채널명으로 필터링
 */
export const getChannelsOptions = (options?: Options<GetChannelsData>) =>
  queryOptions<
    GetChannelsResponse,
    GetChannelsError,
    GetChannelsResponse,
    ReturnType<typeof getChannelsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getChannels({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getChannelsQueryKey(options),
  });

const createInfiniteParams = <
  K extends Pick<QueryKey<Options>[0], 'body' | 'headers' | 'path' | 'query'>,
>(
  queryKey: QueryKey<Options>,
  page: K,
) => {
  const params = { ...queryKey[0] };
  if (page.body) {
    params.body = {
      ...(queryKey[0].body as any),
      ...(page.body as any),
    };
  }
  if (page.headers) {
    params.headers = {
      ...queryKey[0].headers,
      ...page.headers,
    };
  }
  if (page.path) {
    params.path = {
      ...(queryKey[0].path as any),
      ...(page.path as any),
    };
  }
  if (page.query) {
    params.query = {
      ...(queryKey[0].query as any),
      ...(page.query as any),
    };
  }
  return params as unknown as typeof page;
};

export const getChannelsInfiniteQueryKey = (
  options?: Options<GetChannelsData>,
): QueryKey<Options<GetChannelsData>> => createQueryKey('getChannels', options, true);

/**
 * 채널 목록 조회
 *
 * 채널을 페이지 단위로 조회한다. 미지정 시 이름순 12개. name 지정 시 채널명으로 필터링
 */
export const getChannelsInfiniteOptions = (options?: Options<GetChannelsData>) => {
  const opts = infiniteQueryOptions<
    GetChannelsResponse,
    GetChannelsError,
    InfiniteData<GetChannelsResponse>,
    QueryKey<Options<GetChannelsData>>,
    number | Pick<QueryKey<Options<GetChannelsData>>[0], 'body' | 'headers' | 'path' | 'query'>
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<GetChannelsData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  page: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await getChannels({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: getChannelsInfiniteQueryKey(options),
    },
  );
  return opts as Omit<typeof opts, 'initialData'>;
};

export const getChannelQueryKey = (options: Options<GetChannelData>) =>
  createQueryKey('getChannel', options);

/**
 * 채널 상세 조회
 *
 * 채널 단건을 상세 조회한다. 채널 정보와 함께 광고 상품 목록, 오디언스 규모 지표, 집행 사례를 반환한다. 상품이 없는 채널은 products 를 빈 배열로 반환한다.
 */
export const getChannelOptions = (options: Options<GetChannelData>) =>
  queryOptions<
    GetChannelResponse,
    GetChannelError,
    GetChannelResponse,
    ReturnType<typeof getChannelQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getChannel({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getChannelQueryKey(options),
  });
