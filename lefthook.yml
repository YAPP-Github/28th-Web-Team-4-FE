pre-commit:
  parallel: true
  commands:
    lint:
      run: node --run lint -- {staged_files} && git add {staged_files}
      glob: '*.{js,jsx,ts,tsx,mjs,cjs}'
    format:
      run: node --run fmt:check -- --no-error-on-unmatched-pattern {staged_files} && git add {staged_files}
